# Agent Instructions

Before submitting any changes or creating a pull request, you must ensure the following checks pass:

1.  **Run Tests:** Execute `npm run test` to run the test suite. Ensure all tests pass.
2.  **Lint Code:** Execute `npx eslint .` to run the linter. Ensure there are no errors or warnings. Fix any issues found.

Do not submit code that fails these checks.
